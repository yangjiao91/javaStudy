<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.funshion.dobo.base.dao.mapper.UserMapper">

	<resultMap type="com.funshion.dobo.base.dao.entity.User" id="UserResult">
		<id column="id" property="id"/>
		<result column="account" property="account"/>
		<result column="name" property="name"/>
		<result column="is_deleted" property="isDeleted"/>
		<result column="password" property="password"/>
		<result column="email" property="email"/>
		<result column="create_user" property="createUser"/>
		<result column="update_user" property="updateUser"/>
		<result column="create_time" property="createTime"/>
		<result column="update_time" property="updateTime"/>
	</resultMap>
	
	<select id="getUserByAccount" resultMap="UserResult">
		select * from user where account = #{account}
	</select>
	
	<select id="getUserById" resultMap="UserResult">
		select * from user where id = #{id}
	</select>
	
	<select id="getUserAuths" resultType="string">
		select
			distinct b.auth
		from user_role a, role_auth b
		where 
			a.user_id = #{userId} and a.role_id = b.role_id
	</select>
	
	<select id="getUser" resultMap="UserResult">
		select * from user where id = #{id}
	</select>
	
</mapper>
